# Auto-Deploy to GitHub Rule

Whenever any code, configuration, or article file in this workspace is created, updated, or generated:
1. Immediately stage all changes (`git add .`).
2. Commit the changes with a descriptive commit message (`git commit -m "..."`).
3. Push the changes to GitHub (`git push origin main`).
4. Never end a turn with uncommitted or unpushed local changes.
